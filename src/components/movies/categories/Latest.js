import MovieList from "../MovieList";

const Latest = () => {
  return (
    <div id="latest">
      <MovieList movieCategory="latest" categoryTitle="Latest" />
    </div>
  );
};

export default Latest;
